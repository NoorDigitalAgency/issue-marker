name: Publish

on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - Auto Merge
    types:
      - completed

jobs:
  publish:
    uses: NoorDigitalAgency/.github/.github/workflows/publish-action@main
    with:
      branch: main
    secrets:
      token: ${{ secrets.ACTIONS_TOKEN }}
